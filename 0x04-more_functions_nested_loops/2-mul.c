#include "main.h"
/**
 * mul - multiplies two numbers
 * @a: first intiger
 * @b: seconf integer
 *
 * Description: it takes two numbers as arguments
 * and multiplies them
 * Return: it returns the multiplied outcome
 */
int mul(int a, int b)
{
	return (a * b);
}
