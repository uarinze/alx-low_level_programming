#include "main.h"

/**
 * add - adds two numbers
 * @a: first integer
 * @b: second integer
 *
 * Description: it takes two numbers and adds them
 * It returns the value of this addition.
 * Return: it returns the sume of the two numbers
 */
int add(int a, int b)
{
	return (a + b);
}
