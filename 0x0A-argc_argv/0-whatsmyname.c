#include <stdio.h>
#include <stdlib.h>

/**
 * main - prints its name
 * @argc: argument count
 * @argv: argument vector
 *
 * Return: - return 0 (Success)
 */
int main(int argc, char **argv)
{
	printf("%s\n", argv[argc[0]);

	return (0);
}
