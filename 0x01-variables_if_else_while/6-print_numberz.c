#include <stdio.h>
#include <stdlib.h>

/**
 * main - Entry point
 *
 * prints alphabet in reverse order
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char j;

	for (j = 48; j <= 57; j++)
		putchar(j);

	putchar('\n');

	return (0);
}
